build: {gpu: true, cuda: "12.1", system_packages: ["ffmpeg", "git"], python_version: "3.10", python_packages: ["torch==2.3.1", "torchaudio==2.3.1", "transformers==4.44.0", "soundfile", "numpy"], run: ["pip install git+https://github.com/ai4bharat/IndicF5.git", "pip install 'pydantic>=2.7,<3'", {command: "python -c \"from huggingface_hub import login, snapshot_download; login(token=open('/run/secrets/hf_token').read().strip()); snapshot_download('ai4bharat/IndicF5')\"", mounts: [{type: "secret", id: "hf_token", target: "/run/secrets/hf_token"}]}]}
predict: "predict.py:Predictor"
